{% extends 'admin.html.twig' %}
{% block body %}
	<div class="row">
		<div class="col-xl-12 col-lg-12">
			<div class="card">
				<div class="card-header">
					<h4 class="card-title">Atualizar usuário</h4>
				</div>
				<div class="card-body">
					<div class="basic-form">
						<form method="post">
							<div class="form-row">
								{{ include('user/_form.html.twig', {'button_label': 'Atualizar'}) }}
								<a href="{{ path('app_user_index') }}" class="btn btn-default">Voltar</a>
								{{ include('user/_delete_form.html.twig') }}
							</form>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}


{# templates/login/index.html.twig #}
